# =============================================================================
# Traefik Dashboard Router
# =============================================================================
http:
  routers:
    traefik-internal-dashboard:
      entryPoints:
        - websecure
      rule: 'Host(`traefik.mothership.{{ env "DOMAIN" }}`)'
      service: api@internal
      tls:
        certResolver: letsencrypt
