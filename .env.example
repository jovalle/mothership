# =============================================================================
# Mothership Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Domain & Email
# -----------------------------------------------------------------------------
DOMAIN=example.com
ACME_EMAIL=your-email@example.com
TZ=UTC

# -----------------------------------------------------------------------------
# Cloudflare
# -----------------------------------------------------------------------------
# API Token with Zone:DNS:Edit permission
# Get from: https://dash.cloudflare.com/profile/api-tokens
CF_DNS_API_TOKEN=your-cloudflare-api-token

# -----------------------------------------------------------------------------
# Cloudflare Tunnel
# -----------------------------------------------------------------------------
# Get from: https://dash.cloudflare.com/
CLOUDFLARED_TOKEN=your-cloudflared-token

# -----------------------------------------------------------------------------
# Tailscale
# -----------------------------------------------------------------------------
# Auth key from: https://login.tailscale.com/admin/settings/keys
TAILSCALE_AUTH_KEY=tskey-auth-xxxxx

# -----------------------------------------------------------------------------
# CrowdSec
# -----------------------------------------------------------------------------
# Bouncer key (auto-generated on first run by deploy.sh)
CROWDSEC_BOUNCER_KEY=
# Web UI credentials
CROWDSEC_WEB_UI_PASSWORD=

# -----------------------------------------------------------------------------
# AdGuard Home
# -----------------------------------------------------------------------------
# AdGuard admin credentials (optional, for DNS sync)
ADGUARD_USERNAME=admin
ADGUARD_PASSWORD=your-adguard-password

# -----------------------------------------------------------------------------
# AdGuard DNS Sync
# -----------------------------------------------------------------------------
# Internal IP for DNS rewrites (e.g., Tailscale IP, local IP, etc.)
# This is the IP that internal DNS records will point to
INTERNAL_IP=100.100.10.1

# Sync mode: 'watch' for event-driven, 'once' for single sync
DNS_SYNC_MODE=watch

# Log level: DEBUG, INFO, WARNING, ERROR
DNS_SYNC_LOG_LEVEL=INFO

# -----------------------------------------------------------------------------
# Gatus (Status Monitoring)
# -----------------------------------------------------------------------------
# DDNS domain for Plex external checks
DDNS_DOMAIN=your-ddns-domain.net
# Discord webhook for alerts
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx/yyy
# Base64-encoded Basic Auth token for arr-services
# Generate: echo -n "username:password" | base64
GATUS_AUTH_TOKEN=dXNlcm5hbWU6cGFzc3dvcmQ=
# Kromgo stats URL
KROMGO_URL=https://stat.example.com/query?format=endpoint&metric=cluster_memory_usage

# -----------------------------------------------------------------------------
# Beszel Agent
# -----------------------------------------------------------------------------
BESZEL_AGENT_KEY=
BESZEL_AGENT_TOKEN=

# -----------------------------------------------------------------------------
# Arr-Services API Keys
# -----------------------------------------------------------------------------
BAZARR_API_KEY=your-bazarr-api-key
PROWLARR_API_KEY=your-prowlarr-api-key
PROFILARR_API_KEY=your-profilarr-api-key
SABNZBD_API_KEY=your-sabnzbd-api-key

# -----------------------------------------------------------------------------
# Traefik Configuration
# -----------------------------------------------------------------------------
EXTERNAL_IP=
INTERNAL_IP=

# -----------------------------------------------------------------------------
# Pangolin Configuration
# -----------------------------------------------------------------------------
PANGOLIN_GERBIL_BASE_ENDPOINT=
PANGOLIN_SERVER_SECRET=

# -----------------------------------------------------------------------------
# Authelia Configuration
# -----------------------------------------------------------------------------
# Secret used for session encryption (REQUIRED)
# Generate with: openssl rand -hex 32
AUTHELIA_SESSION_SECRET=

# Key used to encrypt data in storage (REQUIRED)
# Generate with: openssl rand -hex 32
AUTHELIA_STORAGE_ENCRYPTION_KEY=

# JWT secret for identity tokens (REQUIRED)
# Generate with: openssl rand -hex 32
AUTHELIA_JWT_SECRET=

# HMAC secret for OIDC (REQUIRED)
# Generate with: openssl rand -hex 32
AUTHELIA_OIDC_HMAC_SECRET=

# OIDC client secret for webapp (REQUIRED)
AUTHELIA_OIDC_CLIENT_WEBAPP_SECRET=

# OIDC issuer private key (REQUIRED)
# Generate with: openssl genrsa 4096
AUTHELIA_OIDC_ISSUER_PRIVATE_KEY=

# Argon2 hashed admin password (REQUIRED)
# Generate with: authelia crypto hash generate argon2
AUTHELIA_USER_ADMIN_PASSWORD_HASH=
